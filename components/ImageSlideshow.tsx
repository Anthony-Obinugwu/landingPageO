"use client";

import AnimatedImageSlideshow, { ImageSlideShowProps } from "./AnimatedImageSlideshow";

export default function ImageSlideshow(props: ImageSlideShowProps) {
  return <AnimatedImageSlideshow {...props} />;
}

